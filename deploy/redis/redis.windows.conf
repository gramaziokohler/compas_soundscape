# Minimal Redis config for local development on Windows.
# Used by: redis-server deploy/redis/redis.windows.conf
# (Memurai Developer or the redis-windows 7.2 BSD build both accept this file.)

bind 127.0.0.1
port 6379
maxmemory 256mb
maxmemory-policy noeviction
save ""
appendonly no
