# =============================================================================
# Backend Dependencies only packages actually imported in backend/ code
# =============================================================================

--extra-index-url https://download.pytorch.org/whl/cu121
--find-links https://download.pytorch.org/whl/torch_stable.html

# --- Web Framework & Server ---
fastapi==0.118.0
uvicorn==0.37.0
python-multipart==0.0.20
python-dotenv==1.1.1
pydantic==2.12.5

# --- HTTP Clients ---
requests==2.32.3
httpx==0.28.1
aiofiles==24.1.0

# --- Auth (Cloudflare Access identity) ---
PyJWT[crypto]==2.10.1

# --- Google Gemini (LLM + TTS) ---
# 2.25.0 is required for the Gemini 3.8 TTS schema: typed speech_metadata
# annotations (SpeechAnnotationParam), response_format AudioResponseFormat, and
# Interaction.output_audio. It also keeps the legacy GenerateContent API
# (client.aio.models.generate_content[_stream]) used by services/llm_service.py.
google-genai==2.25.0
google-auth>=2.56.0
typing-extensions>=4.14.0

# --- Job store ---
redis>=5

# --- Audio: Generation & Processing ---
# Mac will fetch standard wheels, Windows will fetch cu121 wheels
torch==2.4.1+cu121 ; sys_platform != 'darwin'
torchaudio==2.4.1+cu121 ; sys_platform != 'darwin'
torch==2.4.1 ; sys_platform == 'darwin'
torchaudio==2.4.1 ; sys_platform == 'darwin'

diffusers==0.30.0
safetensors==0.4.4
transformers==4.44.2
huggingface-hub==0.24.6
soundfile==0.13.1
librosa==0.11.0
noisereduce==3.0.3
tensorflow==2.18.1
tensorflow-hub==0.16.1
setuptools==78.1.1
importlib

# --- Room Acoustics ---
pyroomacoustics==0.9.0
scipy==1.15.3
numpy==1.26.4
matplotlib==3.10.7

# --- Speckle (3D model platform) ---
# 2026.9.0b3 is required for bundle-only versions: Speckle's 2026.9 file ingestion
# stores uploads as bundles with no legacy object graph, and only this SDK line
# (operations.receive dispatching on `bundle.` refs) can read them. The `bundle`
# extra pulls in pyarrow, which the parquet artifact rail needs.
specklepy[bundle]==2026.9.0b3
gql==4.0.0

# --- Misc ---
numpy-stl==3.2.0
pythonnet==3.0.5
